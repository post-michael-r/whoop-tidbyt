# whoop-tidbyt

A small script that fetches the current [Whoop](https://www.whoop.com/) strain via the Whoop API and displays it on a [Tidbyt](https://tidbyt.com/) device (rendered with Pixlet and pushed via the Tidbyt API).

## Setup

To be filled in as we build.

## How it works

To be filled in as we build.
